app:
    resource: '@AppBundle/Controller/'
    type: annotation
    prefix: /{_locale}
    requirements:
        _locale: %trad%

root:
    path: /
    defaults:
        _controller: FrameworkBundle:Redirect:redirect
        route: accueil
        permanent: false